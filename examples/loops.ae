fn main() -> i32 {
    let mut acc = 0;
    let mut i = 0;
    while i < 10 {
        acc = acc + i;
        i = i + 1;
    }
    for j in 0..5 {
        acc = acc + j;
    }
    print_i32(acc);
    return acc;
}
