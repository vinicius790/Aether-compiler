fn band(a: i32, b: i32) -> i32 {
    // no bitwise ops in 0.2; emulate AND on 0/1 flags
    if a != 0 {
        if b != 0 { return 1; }
    }
    return 0;
}

fn main() -> i32 {
    print_i32(band(1, 1));
    print_i32(band(1, 0));
    return 0;
}
